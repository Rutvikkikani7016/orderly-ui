# Orderly — Frontend

Placeholder for now. Backend and data model come first — we'll scaffold this
(likely Vite + React) once the order sync + reconciliation APIs are working
end-to-end against real seller data.
